version: '3.8'

services:
  polymarket-bot:
    build: .
    container_name: polymarket_bot
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
